# Creating the Jenkins in ec2 using terraform
With the above Terraform code we can create the Jenkins in ec2
Use below command in VS Code
- terraform init
- terraform plan
- terraform apply -auto-approve 
